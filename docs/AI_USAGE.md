# AI Usage Declaration

Declaring AI use does not reduce the grade. You must be able to explain and validate every submitted decision.

| Tool | Activity                                                             | Prompt / purpose | How I validated the result | What I changed / rejected |
|---|----------------------------------------------------------------------|---|---|---|
| Claude | Entender el enunciado del Lab 04                                     | Explicar qué pedía cada punto y cómo abordarlo | Se contrastó contra el PDF del enunciado | N/A |
| Claude | Orientación para tomar decisiones de diseño (BoardRepository, InMemoryBoardRepository, BoardApplicationService) | Entender las opciones posibles y sus implicaciones antes de decidir | Cada decisión se revisó contra los casos de uso reales del PDF | Se quitó existsById de la interfaz; el resto de decisiones se mantuvieron como se plantearon |
| Claude | Redactar ADR-001                                                     | Apoyo para estructurar Context y Decision | Se comparó contra las decisiones ya tomadas | Se acortó el texto |
| Claude | Ayuda para armar la tabla de `api-contract.md` er el bloque de error | Cómo estructurar Request/Success/Error a partir del código ya hecho | Probé cada endpoint yo mismo con PowerShell (POST, GET, PUT y los 3 errores) y comparé las respuestas reales contra la tabla | Reemplacé los ejemplos con los valores reales que obtuve al probar |
| Claude | Auditoría de los pasos 1-5 ya implementados (BoardRepository, InMemoryBoardRepository, BoardApplicationService, controller, manejo de errores) | Revisar consistencia y detectar huecos antes de la entrega | Leí el código y la explicación de por qué `HttpMessageNotReadableException` no queda cubierta por el handler de `IllegalArgumentException` cuando `BoardElement` viola su invariante dentro del body del PUT; validé el razonamiento contra el comportamiento documentado de Jackson/Spring | Agregué el handler de `HttpMessageNotReadableException` en `GlobalExceptionHandler`, actualicé `api-contract.md` con esa nota y limpié los comentarios `TODO LAB-04` ya resueltos en `BoardRepository.java` y `BoardRestController.java` |
| Claude | Escribir y habilitar tests del service (`BoardApplicationServiceTest`, incl. replaceBoard) y crear tests del controlador (`BoardRestControllerTest`) con MockMvc | Cubrir create/get/replace y el caso 'Board no existe' en service y controlador, siguiendo el contrato ya documentado en api-contract.md | Revise a mano cada import, firma de metodo y campo (BoardApplicationService, BoardRestController, CreateBoardRequest, ReplaceBoardRequest, ApiError) contra el codigo fuente real; corri `mvn test` en mi maquina y confirme BUILD SUCCESS con Tests run: 11, Failures: 0, Errors: 0, Skipped: 0 | Ajuste nombres de metodos de test para que describan el escenario; no rechace ningun caso propuesto |
| Claude | Redactar el diagrama de clases (Mermaid) para docs/architecture/ | Generar el diagrama a partir del codigo real (BoardRestController, BoardApplicationService, BoardRepository, InMemoryBoardRepository y el modelo de dominio), sin agregar clases de framework | Compare cada clase y relacion del diagrama contra el codigo fuente de cada archivo | La Vista ArchiMate no se genero con IA (Mermaid no tiene esa notacion): se hizo manualmente en Archi siguiendo una lista de elementos/relaciones que Claude preparo a partir del mismo codigo, y se valido que las relaciones (Serving, Realization, Access) coincidieran con el ADR-001 |


## Lab 05 — Interactive Board (rama `lab5hever`)

| Tool | Activity | Prompt / purpose | How I validated the result | What I changed / rejected |
|---|---|---|---|---|
| Claude | Diagnosticar por que `BoardApplicationServiceTest` no compilaba en `lab5hever` tras el cambio de `BoardElement` (nuevos campos `sourceId`/`targetId` para soportar CONNECTOR) | Encontrar el punto exacto de ruptura y la correccion minima | Revise el constructor compacto nuevo de `BoardElement` y las validaciones de `Board`, y confirme con busqueda en todo el repo que era el unico call-site desactualizado | Se agrego `, null, null` a esa unica llamada; no se toco ninguna otra prueba ni codigo de produccion |
| Claude | Ejecutar `BoardApplicationServiceTest` y `BoardRestControllerTest` para verificar el fix, controlando mi equipo (IntelliJ) con mi autorizacion explicita | Confirmar que el fix no rompia nada mas antes de dar evidencia por buena | Corrimos ambas clases directamente en IntelliJ en mi maquina; vi en pantalla los resultados: 4/4 y 7/7 pruebas en verde | Ningun cambio adicional de codigo; el fix se dejo sin commitear hasta esta entrega |
| Claude | Levantar la app y operar la UI (crear board, agregar y conectar elementos, guardar, forzar recarga completa de pagina y volver a cargar por id) para producir las dos evidencias pedidas en la entrega (board con dos elementos conectados; cambio guardado y persistente tras recargar), controlando mi navegador con mi autorizacion explicita | Cumplir el requisito de evidencia visual sin editar capturas a mano | En cada paso revisamos el estado real devuelto por el backend (mensajes "Board loaded" / "Success", el id persistido) antes de capturar; el archivo final de cada imagen se comparo por hash SHA-256 contra los bytes generados en el navegador para garantizar que no se corrompiera al guardarlo en disco | Para que el conector se viera en la foto se le puso color por JavaScript solo en el DOM en memoria (el repo no incluye `css/app.css`, por eso la linea es invisible por defecto en el navegador); no se modifico ningun archivo del repositorio para lograrlo. Este `app.css` faltante queda como bug real pendiente de reportar al equipo |

## Lab 06 — Real-Time Collaboration, Persona B (rama `Lab6/Hever`)

| Tool | Activity | Prompt / purpose | How I validated the result | What I changed / rejected |
|---|---|---|---|---|
| Claude | Ubicar el starter del Lab 06 y revisar qué faltaba para la parte B | Mi repo local solo tenia el Lab 05; Claude encontro la rama `Lab6/Juli` (starter + parte A ya hecha) y listo los `TODO LAB-06` pendientes y los archivos que faltaban | Compare la lista contra la tabla de reparto (parte B) y el README del starter | Se creo la rama `Lab6/Hever` a partir de `Lab6/Juli`; los TODO de la Persona C (`applyEvent`, `onEvent`) se dejaron sin tocar |
| Claude | Implementar `BoardWebSocketController.handle(...)` | Validar que el `boardId` del destino y del evento coincidan, aplicar el evento con `BoardEventApplicationService` (parte A) y solo si fue aceptado re-difundirlo a `/topic/boards/{boardId}`; los eventos rechazados se registran y no se difunden (`@MessageExceptionHandler`) | `mvn test` en mi maquina: BUILD SUCCESS, Tests run: 20, Failures: 0, Errors: 0 | Se agrego la dependencia `spring-boot-starter-websocket` al `pom.xml`, que faltaba y sin la cual `WebSocketConfig` no compilaba |
| Claude | Implementar `publish(...)` en `board-realtime-client.js` y conectarlo en `app.js` | Rechazar si no hay conexion, si falta `boardId` o si el evento es de otro board; serializar el contrato y enviarlo por STOMP a `/app/boards/{boardId}/events`, dejando los detalles de STOMP dentro del modulo | Claude lo probo con un cliente STOMP simulado en Node (rechazo sin conexion, envio al destino correcto, rechazo por board distinto y por `boardId` faltante) | Se creo `js/events/board-event.js` (fabrica del sobre `BoardEvent` segun `event-contract.md`), porque `app.js` lo importaba pero no existia; se agrego el script de `stomp.js` 2.3.3 en `index.html` porque `window.Stomp` no se cargaba |
| Claude | Crear `BoardEventContractTest` | Probar `ELEMENT_CREATED` y `ELEMENT_MOVED` aplicados y difundidos, el formato JSON del sobre, y que falle si falta `boardId`, si el board no existe, si el `boardId` del destino no coincide o si un move no trae coordenadas | 9/9 pruebas en verde dentro de `mvn test` (20/20 en total) | El test no existia en ninguna rama, se creo desde cero en `infrastructure/web/ws` |
| Claude | Completar ADR-003 (seccion Trade-off) | Redactar lo que se gana y lo que se paga al separar REST (snapshot) de STOMP (eventos en vivo) | Revise que cada punto correspondiera al codigo implementado (validacion antes de difundir, rechazo silencioso, sin versionado hasta el Lab 07) | Se mantuvo la seccion Consequences que ya existia; solo se agrego la tabla de Trade-off |
| Claude | Resolver problemas de git en mi repo local | Diagnosticar el error `cannot lock ref ... Kevyn.lock` del `git pull` | El `.lock` era un archivo vacio del 19 de septiembre (un git que se cerro mal); se verifico que ningun proceso de git estuviera corriendo | Se borro ese `.lock` y se recomendo `git fetch --prune` por el choque de mayusculas `Lab5/Kevyn` vs `lab5/Kevyn` en Windows |

If no AI tool was used, state it explicitly below:


